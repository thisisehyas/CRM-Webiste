// src/main.jsx
import React from "react";
import ReactDOM from "react-dom";
import AppAbout from "./AppForAbout";

ReactDOM.createRoot(document.getElementById("root1")).render(
  <React.StrictMode>
    <AppAbout />
  </React.StrictMode>
);
